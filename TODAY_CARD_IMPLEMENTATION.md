# Todayカード 実装完了レポート

実装日：2026-02-08

## 概要

Study Pro の Todayカード（今日やる一つ）を最小実装しました。押し付けを避けるため、3つの選択肢を必ず提供しています。

## ✅ 成功条件の達成

- ✅ 起動からTodayカードが表示される
- ✅ 3つの操作（これで進める、別案に変える、今日は休む）が成立
- ✅ すべての操作で計測イベントが発火
- ✅ 文言は中立で、強い比喩や断定を避けている

## 📁 実装ファイル

### 新規作成

```
packages/web/                           # Webアプリケーション
├── src/
│   ├── components/
│   │   ├── TodayCard.tsx              # Todayカードコンポーネント（190行）
│   │   └── AlternativeSheet.tsx       # 別案シートコンポーネント（240行）
│   ├── services/
│   │   └── todayTaskService.ts        # タスク生成ロジック（190行）
│   ├── types/
│   │   └── index.ts                   # 型定義（30行）
│   ├── App.tsx                        # メインアプリ（180行）
│   ├── main.tsx                       # エントリーポイント（12行）
│   └── styles.css                     # グローバルスタイル（20行）
├── index.html                         # HTMLテンプレート
├── package.json                       # 依存関係
├── tsconfig.json                      # TypeScript設定
├── vite.config.ts                     # Vite設定
└── README.md                          # ドキュメント
```

## 🎯 主要機能

### 1. Todayカード（TodayCard.tsx）

**表示内容**:
- タスクタイトル
- タスク説明
- 難易度バッジ（1〜3）
- 想定時間バッジ（1/3/10分）

**3つの主要操作**:

#### ① これで進める
```typescript
// 実装
const handleAccept = () => {
  onAccept(task.id);
};

// 発火イベント
tracker.track('today_offer_accept', {
  offer_id: taskId,
  time_budget: currentTask.timeBudget,
});
```

#### ② 別案に変える
```typescript
// 実装
const handleReplaceClick = () => {
  setShowAlternatives(true); // 別案シートを表示
};

// 発火イベント
tracker.track('today_offer_replace', {
  offer_id: taskId,
  replace_reason: reason, // 'too_hard' | 'not_relevant' | 'no_mood' | 'other'
});
```

#### ③ 今日は休む
```typescript
// 実装
const handleSkipClick = () => {
  setShowSkipOptions(true); // 理由選択シートを表示
};

// 発火イベント
tracker.track('today_offer_skip', {
  skip_reason: reason, // 'tired' | 'busy' | 'other'
});
```

### 2. 別案シート（AlternativeSheet.tsx）

**機能**:
- 同等難易度の別タスクを2つ提示
- タスク選択時に理由を選択
- 理由選択後、新しいタスクで Today を更新

**理由選択**:
- 難しすぎる（too_hard）
- 今は関連性が低い（not_relevant）
- 気分が乗らない（no_mood）
- その他（other）

**設計**:
- シート形式で表示（モーダル）
- タップで選択
- キャンセルボタンで閉じる

### 3. タスク生成サービス（todayTaskService.ts）

**MVP実装**:
```typescript
// デフォルトタスク生成
generateDefaultTask(): TodayTask

// 別案生成（同等難易度）
generateAlternatives(currentTask: TodayTask, count: number = 2): TodayTask[]
```

**仮データ構造**:
- 英語学習: 5タスク（単語暗記、リスニング、文法、読解、復習）
- プログラミング: 3タスク（基礎、アルゴリズム、コードレビュー）

**拡張ポイント**:
```typescript
/**
 * 将来の拡張：
 * - AI生成による動的な別案作成
 * - ユーザーの履歴と好みを反映
 * - 週目標との整合性チェック
 * - 時間帯や疲労度を考慮
 */
```

## 📊 計測イベント

すべての操作で以下のイベントが発火します：

| 操作 | イベント名 | プロパティ |
|---|---|---|
| アプリ起動 | `app_open` | entry_point |
| Today提示 | `today_offer_shown` | offer_id, offer_type, difficulty_band, time_budget, offer_source |
| これで進める | `today_offer_accept` | offer_id, time_budget |
| 別案に変える | `today_offer_replace` | offer_id, replace_reason |
| 今日は休む | `today_offer_skip` | skip_reason |

**イベント確認方法**:

```bash
# 開発者ツールのコンソールで確認
[Analytics] today_offer_shown { ... }
[Analytics] today_offer_accept { ... }

# または画面下部のボタンで確認
window.analytics.getEventBuffer()
```

## 🎨 デザイン原則（実装済み）

### 文言の中立性

| ❌ 避けた表現 | ✅ 採用した表現 |
|---|---|
| 「頑張りましょう！」 | 「これで進める」 |
| 「諦めますか？」 | 「今日は休む」 |
| 「もっと簡単なものに変更」 | 「別案に変える」 |

### UI原則

- **1画面1タスク**: 複数のタスクを同時に表示しない
- **選択肢は3つまで**: 主要操作を絞る
- **階層表示**: 別案や理由はシートで表示し、主画面を圧迫しない
- **拒否権の明示**: すべての提案に「別案」「休む」を用意

### 参照

- docs/02_Design.md § 4.1 今日ビュー
- docs/05_Decisions.md DEC-20260208-001（拒否権の必須化）

## 🚀 起動方法

```bash
# Webディレクトリに移動
cd packages/web

# 依存関係をインストール（初回のみ）
npm install

# 開発サーバーを起動
npm run dev
```

ブラウザで `http://localhost:5173` を開きます。

## 🧪 動作確認

### 基本フロー

1. **アプリ起動**
   - Todayカードが表示される
   - タスクタイトル、説明、バッジが表示される
   - 3つのボタンが表示される

2. **これで進める**
   - ボタンをクリック
   - コンソールに `[Analytics] today_offer_accept` が出力
   - アラートで「学習を開始します」と表示

3. **別案に変える**
   - ボタンをクリック
   - 別案シートが表示される
   - 2つの別タスクが表示される
   - タスクを選択 → 理由選択シートが表示
   - 理由を選択 → 新しいタスクが表示される
   - コンソールに `[Analytics] today_offer_replace` と `today_offer_shown` が出力

4. **今日は休む**
   - ボタンをクリック
   - 理由選択シートが表示される
   - 理由を選択
   - コンソールに `[Analytics] today_offer_skip` が出力
   - アラートで「今日は休みます」と表示

### イベント確認

1. 開発者ツールを開く（F12）
2. Console タブを選択
3. `[Analytics]` で始まるログを確認
4. または画面下部の「イベント確認」ボタンをクリック

```javascript
// コンソールで手動確認
window.analytics.getEventBuffer()
// => 発火したイベントの配列が返る
```

## 🔧 拡張ポイント

### 1. AI生成への移行

```typescript
// todayTaskService.ts
async generateAITask(userContext: UserContext): Promise<TodayTask> {
  const response = await openai.createChatCompletion({
    model: 'gpt-4',
    messages: [
      {
        role: 'system',
        content: 'あなたは学習タスクを提案するアシスタントです。',
      },
      {
        role: 'user',
        content: `
          ドメイン: ${userContext.domain}
          週目標: ${userContext.weekGoal}
          最近の履歴: ${userContext.recentTasks}
          疲労度: ${userContext.fatigueLevel}
          
          適切なタスクを1つ提案してください。
        `,
      },
    ],
  });
  
  return parseAIResponse(response);
}
```

### 2. ユーザー履歴の反映

```typescript
// ユーザーの好みを学習
class UserPreferenceService {
  // タスク受諾率を記録
  recordTaskAcceptance(task: TodayTask, accepted: boolean) {
    // 難易度・時間帯・ジャンルごとに受諾率を保存
  }
  
  // 次回の提案に反映
  getPreferredDifficulty(): number {
    // 受諾率が高い難易度を返す
  }
}
```

### 3. 週目標との整合性チェック

```typescript
// 週目標の進捗を考慮
function generateTaskWithGoalAlignment(weekGoal: WeekGoal): TodayTask {
  const progress = calculateWeekProgress(weekGoal);
  
  if (progress.isAhead) {
    // 余裕がある → 難易度を上げる or 新規トピック
    return generateChallengingTask();
  } else if (progress.isBehind) {
    // 遅れている → 短時間タスクで進捗を作る
    return generateQuickTask();
  } else {
    // 順調 → 通常のタスク
    return generateDefaultTask();
  }
}
```

## 📚 参照ドキュメント

- **[packages/web/README.md](./packages/web/README.md)**: Webアプリのドキュメント
- **[docs/02_Design.md](./docs/02_Design.md)**: UX設計仕様 § 4.1 今日ビュー
- **[docs/03_Metrics.md](./docs/03_Metrics.md)**: 計測定義とイベント設計
- **[docs/05_Decisions.md](./docs/05_Decisions.md)**: 意思決定ログ（DEC-20260208-001）
- **[packages/analytics/README.md](./packages/analytics/README.md)**: 計測ライブラリ

## 📝 設計判断

### 別案を2つに限定

**理由**:
- 選択肢が多いと迷いが増える
- シート内に収まる範囲
- 十分な選択肢を提供しつつ、圧迫感を避ける

**参照**: docs/02_Design.md § 4.1「シートで候補を2つ提示」

### 理由選択の必須化

**理由**:
- なぜ差し替えるか/休むかを記録し、提案ロジック改善に活用
- ユーザーの意図を理解する
- 過度な差し替え/休みを防ぐ（軽い摩擦）

**参照**: docs/03_Metrics.md § 6 イベント設計

### 仮データの使用

**理由**:
- MVP段階でAI生成のコストを避ける
- タスク生成ロジックのインターフェースを先に確立
- 拡張ポイントを明示し、将来の実装を容易にする

---

**実装完了日**: 2026-02-08  
**次のステップ**: Focus（学習セッション）の実装
